services:
  postgres:
    image: postgres:16-alpine
    container_name: rise-performance-postgres
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: rise_performance
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
